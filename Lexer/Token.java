package Lexer;

public class Token {
    public String tokenValue; // * keep string for now, conversion takes place at a later stage

    public Token (String _tokenValue) {
        tokenValue = _tokenValue;
    }
}
